apiVersion: apps/v1
kind: Deployment
metadata:
  name: django-scala
  namespace: django-scala-local
  labels:
    app.kubernetes.io/name: django-scala
    app.kubernetes.io/part-of: django-scala-example
spec:
  # The scheduler runs in this Pod. Keep a single replica.
  replicas: 1

  # Avoids two schedulers during a normal rollout.
  strategy:
    type: Recreate

  selector:
    matchLabels:
      app.kubernetes.io/name: django-scala

  template:
    metadata:
      labels:
        app.kubernetes.io/name: django-scala
        app.kubernetes.io/part-of: django-scala-example

    spec:
      terminationGracePeriodSeconds: 45

      securityContext:
        fsGroup: 10001
        fsGroupChangePolicy: OnRootMismatch
        seccompProfile:
          type: RuntimeDefault

      containers:
        - name: django
          image: django-scala/django:dev
          imagePullPolicy: Never

          ports:
            - name: django-http
              containerPort: 8000
              protocol: TCP

          envFrom:
            - configMapRef:
                name: django-scala-config
            - secretRef:
                name: django-scala-secrets

          env:
            - name: POD_NAME
              valueFrom:
                fieldRef:
                  fieldPath: metadata.name
            - name: POD_NAMESPACE
              valueFrom:
                fieldRef:
                  fieldPath: metadata.namespace
            - name: HOME
              value: /tmp

          startupProbe:
            httpGet:
              path: /health/live/
              port: django-http
            periodSeconds: 2
            timeoutSeconds: 2
            failureThreshold: 30

          readinessProbe:
            httpGet:
              path: /health/ready/
              port: django-http
            periodSeconds: 5
            timeoutSeconds: 2
            failureThreshold: 3

          livenessProbe:
            httpGet:
              path: /health/live/
              port: django-http
            periodSeconds: 15
            timeoutSeconds: 2
            failureThreshold: 3

          resources:
            requests:
              cpu: 100m
              memory: 128Mi
            limits:
              cpu: "1"
              memory: 512Mi

          securityContext:
            runAsNonRoot: true
            runAsUser: 10001
            runAsGroup: 10001
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities:
              drop:
                - ALL

          volumeMounts:
            - name: django-tmp
              mountPath: /tmp
            - name: csv-data
              mountPath: /data

        - name: scala-api
          image: django-scala/scala-app:dev
          imagePullPolicy: Never

          ports:
            - name: scala-http
              containerPort: 9000
              protocol: TCP

          envFrom:
            - configMapRef:
                name: django-scala-config
            - secretRef:
                name: django-scala-secrets

          env:
            - name: APP_MODE
              value: server
            - name: POD_NAME
              valueFrom:
                fieldRef:
                  fieldPath: metadata.name
            - name: POD_NAMESPACE
              valueFrom:
                fieldRef:
                  fieldPath: metadata.namespace
            - name: HOME
              value: /tmp

          startupProbe:
            httpGet:
              path: /health/live
              port: scala-http
            periodSeconds: 2
            timeoutSeconds: 2
            failureThreshold: 45

          readinessProbe:
            httpGet:
              path: /health/ready
              port: scala-http
            periodSeconds: 5
            timeoutSeconds: 2
            failureThreshold: 3

          livenessProbe:
            httpGet:
              path: /health/live
              port: scala-http
            periodSeconds: 15
            timeoutSeconds: 2
            failureThreshold: 3

          resources:
            requests:
              cpu: 75m
              memory: 128Mi
            limits:
              cpu: 500m
              memory: 384Mi

          securityContext:
            runAsNonRoot: true
            runAsUser: 10001
            runAsGroup: 10001
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities:
              drop:
                - ALL

          volumeMounts:
            - name: scala-api-tmp
              mountPath: /tmp
            - name: csv-data
              mountPath: /data

        - name: scala-cron
          image: django-scala/scala-app:dev
          imagePullPolicy: Never

          envFrom:
            - configMapRef:
                name: django-scala-config
            - secretRef:
                name: django-scala-secrets

          env:
            - name: APP_MODE
              value: scheduler
            - name: POD_NAME
              valueFrom:
                fieldRef:
                  fieldPath: metadata.name
            - name: POD_NAMESPACE
              valueFrom:
                fieldRef:
                  fieldPath: metadata.namespace
            - name: HOME
              value: /tmp
            - name: CRONTAB_FILE
              value: /etc/supercronic/crontab

          resources:
            requests:
              cpu: 50m
              memory: 96Mi
            limits:
              cpu: 500m
              memory: 384Mi

          securityContext:
            runAsNonRoot: true
            runAsUser: 10001
            runAsGroup: 10001
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities:
              drop:
                - ALL

          volumeMounts:
            - name: scala-crontab
              mountPath: /etc/supercronic
              readOnly: true
            - name: scala-cron-tmp
              mountPath: /tmp
            - name: csv-data
              mountPath: /data

      volumes:
        - name: scala-crontab
          configMap:
            name: scala-crontab
            defaultMode: 0444
            items:
              - key: crontab
                path: crontab

        - name: csv-data
          persistentVolumeClaim:
            claimName: shared-csv-data

        - name: django-tmp
          emptyDir:
            sizeLimit: 128Mi

        - name: scala-api-tmp
          emptyDir:
            sizeLimit: 128Mi

        - name: scala-cron-tmp
          emptyDir:
            sizeLimit: 128Mi
