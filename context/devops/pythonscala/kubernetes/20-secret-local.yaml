apiVersion: v1
kind: Secret
metadata:
  name: django-scala-secrets
  namespace: django-scala-local
type: Opaque
stringData:
  # Local development value only. Do not reuse in production.
  DJANGO_SECRET_KEY: "minikube-local-development-secret-key"
