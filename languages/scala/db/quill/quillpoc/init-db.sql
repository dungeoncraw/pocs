CREATE TABLE IF NOT EXISTS person (
                                      name VARCHAR(255) NOT NULL,
    age INT NOT NULL
    );

INSERT INTO person (name, age) VALUES
                                   ('John Doe', 30),
                                   ('Jane Smith', 25);
