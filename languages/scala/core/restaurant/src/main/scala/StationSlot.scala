final case class StationSlot(
                              id: String,
                              stationId: String,
                              availableAtMinute: Int
                            )